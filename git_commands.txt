# Continue working normally now:
git add .

Git status  # to see the status

git commit -m "Your message"

git push origin main  # No force needed anymore



git rm --cached file1.txt
git commit -m "remove file1.txt"